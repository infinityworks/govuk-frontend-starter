const { router: cookieRoutes } = require("./cookies");
const { router: proseRoutes } = require("./prose");
const { router: questionPagesRoutes } = require("./question-pages");

module.exports = {
  cookieRoutes,
  proseRoutes,
  questionPagesRoutes,
};
